/* Select the year: */
SELECT DATE_FORMAT(last_update, "%Y") AS year FROM actor;

/* Select the month: */
SELECT DATE_FORMAT(last_update, "%M") AS year FROM actor;

/* Format date: */
SELECT DATE_FORMAT(last_update, "%d/%m/%y") AS year FROM actor;
